name: flutter_tdd_clean_architecture_course
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Service locator
  get_it: 
  # Bloc for state management
  flutter_bloc: 
  # Value equality
  equatable: 
  # Functional programming thingies
  dartz: 
  # Remote API
  data_connection_checker: 
  http: 
  # Local cache
  shared_preferences: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: 

flutter:
  uses-material-design: true
